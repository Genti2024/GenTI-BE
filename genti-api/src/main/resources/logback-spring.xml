<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <property name="CONSOLE_LOG_PATTERN"
              value="[%d{yyyy-MM-dd HH:mm:ss}:%-4relative] %green([%thread]) %highlight(%-5level) %boldWhite([%C.%M:%yellow(%L)]) - %msg%n"/>
    <property name="FILE_LOG_PATTERN"
              value="[%d{yyyy-MM-dd HH:mm:ss}:%-4relative] [%thread] %-5level [%C.%M:%L] - %msg%n"/>
    <springProperty name="BASE_URL" source="discord.webhook-url.base"/>
    <springProperty name="ADMIN_PATH" source="discord.webhook-url.admin"/>
    <springProperty name="EVENT_PATH" source="discord.webhook-url.event"/>
    <springProperty name="ERROR_PATH" source="discord.webhook-url.error"/>

    <springProfile name="local">
        <include resource="console-appender.xml"/>
        <include resource="discord-appender.xml"/>
        <include resource="file-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="ASYNC_DISCORD"/>
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

    <springProfile name="staging">
        <include resource="console-appender.xml"/>
        <include resource="discord-appender.xml"/>
        <include resource="file-appender.xml"/>

        <root level="INFO">
            <appender-ref ref="ASYNC_DISCORD"/>
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

    <springProfile name="deploy">
        <include resource="console-appender.xml"/>
        <include resource="discord-appender.xml"/>
        <include resource="file-appender.xml"/>

        <root level="INFO">
            <appender-ref ref="ASYNC_DISCORD"/>
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

</configuration>